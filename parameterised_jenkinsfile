pipeline {
		agent any 
		parameters {
			string(name: 'Greeting', defaultValue: 'parameteri pipline', description: 'parameterized pipeline')
		}
				stages {
						stage('One') {
								steps {
										echo "${params.Greeting} example!!"
								}
						}
						}
						}
						
		